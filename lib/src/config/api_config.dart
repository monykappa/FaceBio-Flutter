class ApiConfig {
  static const String baseUrl = 'https://face.chlat.dev';

  // If you already have a working `Authorization` header from FastAPI,
  // set it here (for example: Basic xxxxxx). Leave empty to use username/password.
  static const String basicAuthHeaderOverride = '';

  // Replace with your actual credentials when override is empty.
  static const String basicAuthUsername = 'adminFacebioAuth';
  static const String basicAuthPassword = 'bioFaceacleda\$121120\$';
}
